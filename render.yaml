services:
  - type: web
    name: puntlens-bot
    env: node
    buildCommand: npm install
    startCommand: node src/index.js
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
